# 2026-09-14

## Fixed

- `[server]` Add an explicit game_control_allow_remote opt-in for isolated container networks; default loopback restriction and mandatory bearer authentication remain enforced.
