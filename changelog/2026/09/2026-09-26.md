# 2026-09-26

## Fixed

- Map → Char、Char → Login 每次断线重连前重新解析配置的服务名，避免容器重启或重建后持续连接旧 IP；DNS 暂时不可用时保留服务名并等待下一轮重试，启动时不再将解析失败的上游服务名覆盖为本机地址。

## Performance

- 接收移动端摇杆停止请求 `0xd03`，沿服务器当前行走路线尽快停步，并同步本人和附近玩家；不采用客户端坐标，不强制回到格子中心，避免松手后继续走完整段路线。强制行走、冲刺及追击／技能移动保持原有行为。
