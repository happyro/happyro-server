//===== rAthena Script =======================================
//= Thief Mage Quest Story Mode
//===== By: ==================================================
//= Fix up by Jukka
//===== Current Version: =====================================
//= 2.0
//===== Compatible With: =====================================
//= rAthena Project
//===== Description: =========================================
//= [Official Conversion]
//= Battle Traces from Thief and Magician training quests.
//===== Additional Comments: =================================
//= 1.0 Fully working
//= 1.1 optimized [Lupus]
//= 1.2 Changed "battle_trace" to "Trace of Battle". [L0ne_W0lf]
//= 1.3 Misc. updates. [L0ne_W0lf]
//= 2.0 Clean-up. [Capuche]
//============================================================

// Battle Traces 1
//============================================================
moc_fild12,166,369,0	script	Trace of Battle#1	CLEAR_NPC,{
	.@class_thief = ( BaseClass == Job_Thief );
	.@class_mage = ( BaseClass == Job_Mage );
	if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
		mes "^3355FF这里有迹象表明";
		mes "这里似乎发生过激烈冲突，";
		mes "但你无法确定具体发生了什么……^000000";
		} else if ((tu_thief01 < 5 && .@class_thief) || (.@class_mage && tu_magician01 < 8)) {
		mes "^3355FF你发现了毒药的痕迹，";
	mes "战斗中使用过的毒药。你感受到它的威力，决定不要靠太近。^000000";
		if (.@class_mage)
		specialeffect EF_VENOMDUST2;
		} else if (tu_thief01 == 5 && .@class_thief) {
		mes "^3355FFYou find traces of poison";
		mes "战斗中使用过的毒药。调查现场后，你感到一阵强烈眩晕。^000000";
			tu_thief01 = 6;
	sc_start SC_Poison,60000,0;
		specialeffect EF_VENOMDUST2;
		} else if (tu_thief01 == 6 && .@class_thief) {
		mes "^3355FFYou find traces of poison";
		mes "战斗中使用过的毒药。调查现场后，你感到一阵强烈眩晕。^000000";
		sc_start SC_Poison,60000,0;
		specialeffect EF_VENOMDUST2;
		} else if (tu_thief01 == 7 && .@class_thief) {
	mes "^3355FFYou find traces of poison";
		mes "战斗中使用过的毒药。你感受到它的威力，决定不要靠太近。^000000";
		} else if ((.@class_thief && tu_thief01 == 8) || (.@class_mage && tu_magician01 == 8)) {
		mes "^3355FF这里有战斗的痕迹，";
		mes "战斗痕迹通向不同方向。从弥漫在此处的怪异气味看，似乎使用过某种致命毒药。^000000";
		next;
		mes "^3355FF不过……";
	mes "这场战斗的痕迹明显向南延伸。^000000";
		if (tu_magician01 == 8)
		tu_magician01 = 9;
		else
		tu_thief01 = 9;
	} else {
		mes "^3355FF你发现了";
		mes "一场激烈追逐的痕迹，";
		mes "正向南方延伸。";
		}
		close;
		}
		// Battle Traces 2
		//============================================================
		moc_fild12,173,215,0	script	Trace of Battle#2	CLEAR_NPC,{
		.@class_thief = ( BaseClass == Job_Thief );
		.@class_mage = ( BaseClass == Job_Mage );
		if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
			mes "^3355FF这里有迹象表明 that show";
		mes "这里似乎发生过激烈冲突，";
			mes "但你无法确定具体发生了什么……^000000";
	} else if ((tu_thief01 < 9 && .@class_thief) || (tu_magician01 < 9 && .@class_mage)) {
		mes "^3355FF这里有迹象表明";
		mes "许多人经过这片区域。^000000";
		} else if ((tu_thief01 == 9 && .@class_thief) || (tu_magician01 == 9 && .@class_mage)) {
	mes "^3355FFYou've found traces";
	mes "追逐痕迹继续向东延伸。^000000";

if (tu_magician01 == 9)
tu_magician01 = 10;
else
tu_thief01 = 10;
	} else {
	mes "^3355FFThese traces of";
	mes "追逐痕迹通向东方。^000000";
		}
		close;
		}
		// Battle Traces 3
		//============================================================
	moc_fild12,276,165,0	script	Trace of Battle#3	CLEAR_NPC,{
		.@class_thief = ( BaseClass == Job_Thief );
		.@class_mage = ( BaseClass == Job_Mage );
		if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
		mes "^3355FF这些看起来像";
	mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
		} else if ((tu_thief01 < 10 && .@class_thief) || (tu_magician01 < 10 && .@class_mage)) {
		mes "^3355FF这里有迹象表明";
		mes "许多人经过这片区域。^000000";
		} else if ((tu_thief01 == 10 && .@class_thief) || (tu_magician01 == 10 && .@class_mage)) {
			mes "^3355FFYou examine these traces";
		mes "你注意到一组脚印格外清晰，仿佛特意留下让人发现。^000000";
			if (tu_magician01 == 10)
	tu_magician01 = 11;
		else
		tu_thief01 = 11;
		} else {
	mes "^3355FFIt's a very";
	mes "一组看起来很奇怪的脚印。^000000";

}
close;
}
// Battle Traces 4
	//============================================================
	moc_fild11,39,163,0	script	Trace of Battle#4	CLEAR_NPC,{
	.@class_thief = ( BaseClass == Job_Thief );
		.@class_mage = ( BaseClass == Job_Mage );
		if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
		mes "^3355FF这里有迹象表明 that show";
		mes "这里似乎发生过激烈冲突，但你无法确定具体发生了什么……^000000";
	} else if ((tu_thief01 < 11 && .@class_thief) || (tu_magician01 < 11 && .@class_mage)) {
		mes "^3355FF这里有迹象表明";
		mes "许多人经过这片区域。^000000";
		} else if ((tu_thief01 == 11 && .@class_thief) || (tu_magician01 == 11 && .@class_mage)) {
		mes "^3355FFFrom these traces,";
	mes "你发现又多了一组脚印，似乎有其他人介入。";
		mes "这些脚印明显更清晰，也小得多。^000000";
		if (tu_magician01 == 11)
		tu_magician01 = 12;
		else
		tu_thief01 = 12;
		} else {
			mes "^3355FFFrom these traces,";
		mes "你发现又有人加入了这场激烈冲突。^000000";
			}
	close;
		}
		// Battle Traces 5
		//============================================================
	moc_fild11,205,52,0	script	Trace of Battle#5	CLEAR_NPC,{
	
.@class_thief = ( BaseClass == Job_Thief );
.@class_mage = ( BaseClass == Job_Mage );
if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
mes "^3355FFThese look like";
mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
	} else if ((tu_thief01 < 12 && .@class_thief) || (tu_magician01 < 12 && .@class_mage)) {
	mes "^3355FF许多人经过这片区域。^000000";
	} else if ((tu_thief01 == 12 && .@class_thief) || (tu_magician01 == 12 && .@class_mage)) {
		mes "^3355FF战斗痕迹现在分向南北。不过，^000000";
		if (countitem(506) == 0) {// Green_Potion
		mes "^3355FF这里有一滩强烈毒液，必须先中和才能调查现场。^000000";
		close;
		}
	mes "^3355FF这里有一滩强烈毒液，必须先中和才能调查现场。^000000";
		next;
		mes "^3355FF幸运的是，你可以使用一瓶绿药水暂时中和毒液。^000000";
		next;
		switch(select("使用绿药水。:不使用。")) {
	case 1:
		mes "^3355FF毒液变弱并蒸发了一部分，露出藏在水洼中的一块布。^000000";
		next;
		switch(select("不调查。:调查。")) {
		case 1:
		mes "["+strcharinfo(0)+"]";
		mes "一块布而已，没什么好在意的。";
		close;
		case 2:
			mes "["+strcharinfo(0)+"]";
		mes "嘿……这块布上有血，还有我看不懂的文字。";
			mes "希望能提供一些线索吧？";
	if (tu_thief01 == 12)
		tu_thief01 = 13;
		else
		tu_magician01 = 13;
		next;
	mes "^3355FF你从毒液水洼中取出布片，收了起来。^000000";
	close;

}
case 2:
mes "["+strcharinfo(0)+"]";
mes "嗯……沿着痕迹走，应该比停下来调查更快。";
	close;
	}
	} else {
		mes "^3355FF你找不到";
		mes "除了分向南北的痕迹，你在这里找不到其他东西。^000000";
		}
		close;
	}
		// Battle Traces 6
		//============================================================
		moc_fild11,226,235,0	script	Trace of Battle#6	CLEAR_NPC,{
		.@class_thief = ( BaseClass == Job_Thief );
	.@class_mage = ( BaseClass == Job_Mage );
		if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
		mes "^3355FF这些看起来像";
		mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
		} else if ((tu_thief01 < 13 && .@class_thief) || (tu_magician01 < 13 && .@class_mage)) {
			mes "^3355FF这里有迹象表明";
			mes "许多人经过这片区域。^000000";
		} else {
		mes "^3355FF这些脚印";
		mes "看起来正从南方朝北方前进，";
		mes "但现在还无法确定。^000000";
		}
		close;
		}
		// Battle Traces 7
		//============================================================
		moc_fild11,184,342,0	script	Trace of Battle#7	CLEAR_NPC,{
			.@class_thief = ( BaseClass == Job_Thief );
			.@class_mage = ( BaseClass == Job_Mage );
			if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
			mes "^3355FF这些看起来像";
			mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
			} else if ((tu_thief01 < 13 && .@class_thief) || (tu_magician01 < 13 && .@class_mage)) {
			mes "^3355FF这里有迹象表明";
				mes "许多人经过这片区域。^000000";
				} else {
				mes "^3355FFThe trail here looks pretty";
				mes "这里的痕迹很混乱，似乎曾长时间战斗。";
				mes "但脚印明显朝南。^000000";
			}
				close;
				}
				// Battle Traces 8
				//============================================================
				moc_fild17,213,358,0	script	Trace of Battle#8	CLEAR_NPC,{
				.@class_thief = ( BaseClass == Job_Thief );
				.@class_mage = ( BaseClass == Job_Mage );
				if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
					mes "^3355FF这些看起来像";
				mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
					} else if ((tu_thief01 < 13 && .@class_thief) || (tu_magician01 < 13 && .@class_mage)) {
				mes "^3355FF这里有两组";
				mes "这片区域有两组明显的脚印，";
				mes "但现在看不出特别意义。^000000";
				} else if ((tu_thief01 == 13 && .@class_thief) || (tu_magician01 == 13 && .@class_mage)) {
				mes "^3355FF在这附近，";
			mes "看起来这里只有两组脚印。";
		mes "之前发现的另一组去哪了？^000000";
			if (tu_magician01 == 13)
			tu_magician01 = 14;
			else
			tu_thief01 = 14;
			} else {
			mes "^3355FF根据证据，";
			mes "根据你在这里找到的证据，战斗从此处起似乎只涉及两个人。^000000";
		}
	close;
		}
		// Battle Traces 9
		//============================================================
		moc_fild17,228,274,0	script	Trace of Battle#9	CLEAR_NPC,{
		
	.@class_thief = ( BaseClass == Job_Thief );
	.@class_mage = ( BaseClass == Job_Mage );
if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
mes "^3355FFThese look like";
mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
} else if ((tu_thief01 < 14 && .@class_thief) || (tu_magician01 < 14 && .@class_mage)) {
mes "^3355FFTraces of some";
	mes "某种战斗的痕迹散布在这片区域各处。^000000";
	} else if ((tu_thief01 == 14 && .@class_thief) || (tu_magician01 == 14 && .@class_mage)) {
	mes "^3355FF你沿着痕迹前进，";
		mes "虽然北方和东南方的痕迹混在一起，战斗似乎仍向东继续。";
		mes "但你应该再仔细检查这里……^000000";
		if (tu_magician01 == 14)
		tu_magician01 = 15;
	else
		tu_thief01 = 15;
		} else if ((tu_thief01 == 15 && .@class_thief) || (tu_magician01 == 15 && .@class_mage)) {
		if (rand(1,10) == 7) {
		mes "^3355FF进一步调查后，";
	mes "更仔细地调查后，你找到另一块染血的布。";
		mes "你决定带在身上，希望它能提供更多线索。^000000";
		if (tu_magician01 == 15)
		tu_magician01 = 16;
		else
		tu_thief01 = 16;
		} else {
	
	mes "^3355FFYou don't find anything,";
mes "但你仍觉得这里有重要线索尚未发现。继续调查也无妨。^000000";
}
} else {
mes "^3355FFYou better continue";
mes "沿着向西延伸的痕迹继续前进。^000000";
	}
	close;
	}
		// Battle Traces 10
		//============================================================
		moc_fild17,34,292,0	script	Trace of Battle#10	CLEAR_NPC,{
		.@class_thief = ( BaseClass == Job_Thief );
	.@class_mage = ( BaseClass == Job_Mage );
		if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
		mes "^3355FF这些看起来像";
		mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
		} else if ((tu_thief01 < 16 && .@class_thief) || (tu_magician01 < 16 && .@class_mage)) {
	mes "^3355FF这里有迹象表明";
		mes "显示之前有很多人来过这里。^000000";
		} else if ((tu_thief01 == 16 && .@class_thief) || (tu_magician01 == 16 && .@class_mage)) {
		mes "^3355FFThe lead set of footprints,";
		mes "可能属于被追赶者的脚印变得更加凌乱，仿佛已经精疲力竭、陷入绝望。这些痕迹通向西方。^000000";
		if (tu_magician01 == 16)
	
	tu_magician01 = 17;
else
tu_thief01 = 17;
} else {
mes "^3355FFThe trail from";
mes "从这里向西延伸。^000000";
	}
	close;
	}
		// Battle Traces 11
		//============================================================
		moc_fild18,346,296,0	script	Trace of Battle#11	CLEAR_NPC,{
		.@class_thief = ( BaseClass == Job_Thief );
	.@class_mage = ( BaseClass == Job_Mage );
		if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
		mes "^3355FF这些看起来像";
		mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
		} else if ((tu_thief01 < 17 && .@class_thief) || (tu_magician01 < 17 && .@class_mage)) {
		mes "^3355FF这里有迹象表明";
		mes "显示之前有很多人来过这里。^000000";
	} else if ((tu_thief01 == 17 && .@class_thief) || (tu_magician01 == 17 && .@class_mage)) {
		mes "^3355FF沿着";
		mes "沿着痕迹，你发现这里有血迹。看起来有人在此受了重伤。^000000";
		if (tu_magician01 == 17)
		tu_magician01 = 18;
		else
		tu_thief01 = 18;
			} else {
		mes "^3355FFSince someone involved";
			mes "由于冲突中的某人正在流血，现在或许更容易找到追逐的痕迹。^000000";
	}
		close;
		}
		// Battle Traces 12
		//============================================================
		
	moc_fild18,309,257,0	script	Trace of Battle#12	CLEAR_NPC,{
	.@class_thief = ( BaseClass == Job_Thief );
.@class_mage = ( BaseClass == Job_Mage );
if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
mes "^3355FFThese look like";
mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
} else if ((tu_thief01 < 18 && .@class_thief) || (tu_magician01 < 18 && .@class_mage)) {
	mes "^3355FF这里有迹象表明";
	mes "显示之前有很多人来过这里。^000000";
	} else if ((tu_thief01 == 18 && .@class_thief) || (tu_magician01 == 18 && .@class_mage)) {
		mes "^3355FFThese traces lead";
		mes "向北延伸，看起来追赶者正越来越接近猎物。^000000";
		if (tu_magician01 == 18)
		tu_magician01 = 19;
	else
		tu_thief01 = 19;
		} else {
		+// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
			// 切片行数保留
		// 切片行数保留
			// 切片行数保留
	// 切片行数保留
		// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
				// 切片行数保留
			// 切片行数保留
				// 切片行数保留
		// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	
	// 切片行数保留
// 切片行数保留
// 切片行数保留
// 切片行数保留
// 切片行数保留
// 切片行数保留
	// 切片行数保留
	// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		+// 切片行数保留
			// 切片行数保留
		// 切片行数保留
			// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
	
// 切片行数保留
// 切片行数保留
// 切片行数保留
// 切片行数保留
// 切片行数保留
	// 切片行数保留
	// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
			// 切片行数保留
		// 切片行数保留
			// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
	// 切片行数保留

// 切片行数保留
// 切片行数保留
// 切片行数保留
// 切片行数保留
	// 切片行数保留
	// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
			// 切片行数保留
		// 切片行数保留
			// 切片行数保留
	// 切片行数保留
		mes "^3355FF这条痕迹";
		mes "追逐现在向北延伸。^000000";
		}
	close;
	}

// Battle Traces 13
//============================================================
moc_fild18,177,333,0	script	Trace of Battle#13	CLEAR_NPC,{
.@class_thief = ( BaseClass == Job_Thief );
	.@class_mage = ( BaseClass == Job_Mage );
	if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
	mes "^3355FFThese look like";
		mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
		} else if (tu_thief01 < 19 && .@class_thief) {
		mes "^3355FF这里有迹象表明";
		mes "显示之前有很多人来过这里。^000000";
	} else if (tu_magician01 < 19 && .@class_mage) {
		mes "看起来有一大群人经过。";
		} else if ((tu_thief01 == 19 && .@class_thief) || (tu_magician01 == 19 && .@class_mage)) {
		mes "^3355FFJudging from these";
		mes "从痕迹看，似乎有更多人加入战斗，战斗现在朝西南方向延伸。^000000";
	if (tu_magician01 == 19)
		tu_magician01 = 20;
	else
		tu_thief01 = 20;
		} else {
		mes "^3355FFIt looks like";
		mes "战斗从这里朝西南方向继续。^000000";
		}
		close;
		}
			// Battle Traces 14
		//============================================================
			moc_fild18,111,303,0	script	Trace of Battle#14	CLEAR_NPC,{
	.@class_thief = ( BaseClass == Job_Thief );
		.@class_mage = ( BaseClass == Job_Mage );
		if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
		mes "^3355FFThese look like";
		mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
	} else if ((tu_thief01 < 20 && .@class_thief) || (tu_magician01 < 20 && .@class_mage)) {
	mes "^3355FF这里有迹象表明";

mes "显示之前有很多人来过这里。^000000";
} else if ((tu_thief01 == 20 && .@class_thief) || (tu_magician01 == 20 && .@class_mage)) {
	mes "^3355FF在这片区域，看起来";
mes "看起来更多人加入了战斗，追逐明显向南方延伸。^000000";
	if (tu_magician01 == 20)
	tu_magician01 = 21;
	else
		tu_thief01 = 21;
		} else {
		mes "^3355FFThe trail of";
		mes "这场战斗向南方延伸。^000000";
	}
		close;
		}
		// Battle Traces 15
		//============================================================
	moc_fild18,109,197,0	script	Trace of Battle#15	CLEAR_NPC,{
		.@class_thief = ( BaseClass == Job_Thief );
		.@class_mage = ( BaseClass == Job_Mage );
		if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
		mes "^3355FFThese look like";
		mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
		} else if (tu_thief01 < 21 && .@class_thief) {
		mes "^3355FF这里有迹象表明";
		mes "显示之前有很多人来过这里。^000000";
			} else if (tu_magician01 < 21 && .@class_mage)
	mes "看起来有一大群人经过。";
		else if ((tu_thief01 == 21 && .@class_thief) || (tu_magician01 == 21 && .@class_mage)) {
		mes "^3355FF这片区域显然";
		mes "这里明显发生过激烈战斗，地面到处散落着毒药痕迹。^000000";
	next;
	switch(select("Continue following the traces.:Further investigate the area.")) {

case 1:
	mes "^3355FF你检查了";
mes "虽然有痕迹，但无法辨认战斗继续的方向……^000000";
close;
	case 2:
	mes "^3355FFYou find a bunch of";
	mes "这里有使用不同毒药的陷阱。已经触发的和未触发的混在一起。^000000";
		if (tu_thief01 == 21)
		tu_thief01 = 22;
		else
		tu_magician01 = 22;
	close;
		}
		} else if ((tu_thief01 == 22 && .@class_thief) || (tu_magician01 == 22 && .@class_mage)) {
		if (rand(1,3) == 3) {
		mes "^3355FFOne trap in particular";
	mes "其中一个比其他陷阱更显眼。也许应该先倒些绿药水中和它。^000000";
		next;
	switch(select("Pour Green Potion.:Don't use Green Potion.")) {
		case 1:
		if (countitem(506) < 1) {// Green_Potion
		mes "^3355FF遗憾的是，你";
		mes "你没有可以倒在这个陷阱上的绿药水……^000000";
		close;
		} else {
		mes "^3355FF将那东西倒入";
		mes "绿药水似乎没有效果。也许应该试试别的东西。^000000";
			delitem 506,1; //Green_Potion
			if (tu_thief01 == 22)
			tu_thief01 = 23;
			else
			tu_magician01 = 23;
			close;
		}
			case 2:
			mes "["+strcharinfo(0)+"]";
			mes "我想……再调查一下这片区域？";
			close;
			}
			} else {
			mes "^3355FFBy sheer accident,";
				mes "你触发了这里的一个陷阱。^000000";
			sc_start SC_Poison,60000,0;
				specialeffect EF_VENOMDUST2;
			percentheal -30,0;
		}
	} else if ((tu_thief01 == 23 && .@class_thief) || (tu_magician01 == 23 && .@class_mage)) {
		if (countitem(511) > 0 && countitem(716) > 0) {
			mes "^3355FFYou try grinding";
			mes "你将绿草撒在陷阱上，再放上一颗红色宝石。";
			mes "宝石发光，慢慢融化……^000000";
			next;
		mes "^3355FF虽然你没有";
			mes "虽然你并不完全理解毒药和解毒剂的原理，但成功拆除了陷阱。现在可以安全检查里面有什么了。^000000";
			delitem 511,1; //Green_Herb
			delitem 716,1; //Red_Gemstone
			if (tu_thief01 == 23)
				tu_thief01 = 24;
					else
					tu_magician01 = 24;
					} else if (countitem(511) > 0) {
					mes "^3355FF你应该使用其他与解毒或毒药有关的催化剂拆除陷阱。你有绿草，于是将它撒在陷阱上。^000000";
					next;
				mes "^3355FF但什么也没发生。看来除了绿草外，还需要另一种催化剂才能";
					mes "拆除这个陷阱。^000000";
					} else if (countitem(716) > 0) {
					mes "^3355FF你应该使用其他与解毒或毒药有关的催化剂来拆除陷阱。你有红色宝石，于是将它磨碎撒在陷阱上。^000000";
					next;
					mes "^3355FF但什么也没发生。看来还需要另一种催化剂配合红色宝石才能拆除陷阱。^000000";
					} else {
					mes "^3355FF你应该使用其他与解毒或毒药有关的催化剂拆除陷阱。但该带什么物品呢？^000000";
						}
					} else if ((tu_thief01 == 24 && .@class_thief) || (tu_magician01 == 24 && .@class_mage)) {
						mes "^3355FF在拆除的陷阱内部，你找到另一块染血的奇怪布片。你把它带走，希望能成为某种证据。^000000";
					if (tu_thief01 == 24)
				tu_thief01 = 25;
			else
				tu_magician01 = 25;
				} else {
				mes "^3355FF你继续调查这片区域，猜测战斗可能向南方发展。^000000";
				}
				close;
			}
		// Battle Traces 16
			//============================================================
			moc_fild18,156,96,0	script	Trace of Battle#16	CLEAR_NPC,{
			.@class_thief = ( BaseClass == Job_Thief );
			.@class_mage = ( BaseClass == Job_Mage );
			if ((.@class_thief == 0 && .@class_mage == 0) || (eaclass()&EAJL_BABY)) {
			mes "^3355FF这些看起来像";
		mes "某种追逐或战斗的痕迹，但无法确定究竟是什么。^000000";
	} else if ((tu_thief01 < 25 && .@class_thief) || (tu_magician01 < 25 && .@class_mage)) {
		mes "^3355FF这里有迹象显示";
			mes "一大群人不知为何曾来过这片区域。^000000";
			} else if ((tu_thief01 == 25 && .@class_thief) || (tu_magician01 == 25 && .@class_mage)) {
			mes "^3355FF这些痕迹通向悬崖边，标志着追踪的终点。看来被追赶的人在这里遭遇了命运。^000000";
			next;
			mes "^3355FF许多脚印通向悬崖边并离开现场，只有一组脚印明显属于一直被追赶的人……^3355FF";
			next;
			mes "^3355FF看来你已经从调查中了解到能发现的一切。最好尽快回去向伊尔汗报告。^000000";
			if (tu_magician01 == 25)
			tu_magician01 = 26;
			else
			tu_thief01 = 26;
			} else {
			mes "^3355FF这里就是痕迹的终点。已经没有更多战斗痕迹可找了。^000000";
			}
			close;
			}
			+// 切片行数保留
			// 切片行数保留
				// 切片行数保留
			// 切片行数保留
				// 切片行数保留
		// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
		// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
		// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
			// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
			// 切片行数保留
		// 切片行数保留
			// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
	// 切片行数保留

// 切片行数保留
// 切片行数保留
// 切片行数保留
// 切片行数保留
	// 切片行数保留
	// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
			// 切片行数保留
		// 切片行数保留
			// 切片行数保留
	// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
		// 切片行数保留
	// 切片行数保留
	// 切片行数保留
// 切片行数保留
